"""
Copyright (C) 2023-2026 University of Münster.

datasafe-RDM is free software; you can redistribute it and/or
modify it under the terms of the MIT License; see LICENSE file for more
details.

InvenioRDM settings for datasafe-RDM project.

This file was automatically generated by 'invenio-cli init'.

For the full list of settings and their values, see
https://inveniordm.docs.cern.ch/reference/configuration/.
"""

from os import environ
from ast import literal_eval
from datetime import datetime
from invenio_i18n import lazy_gettext as _


def _(x):  # needed to avoid start time failure with lazy strings
    return x


# Flask
# =====
# See https://flask.palletsprojects.com/en/1.1.x/config/

# Define the value of the cache control header `max-age` returned by the server when serving
# public files. Files will be cached by the browser for the provided number of seconds.
# See flask documentation for more information:
# https://flask.palletsprojects.com/en/2.1.x/config/#SEND_FILE_MAX_AGE_DEFAULT
SEND_FILE_MAX_AGE_DEFAULT = 300

# SECURITY WARNING: keep the secret key used in production secret!
# Do not commit it to a source code repository.
# TODO: Set
SECRET_KEY = "CHANGE_ME"

# Since HAProxy and Nginx route all requests no matter the host header
# provided, the allowed hosts variable is set to localhost. In production it
# should be set to the correct host and it is strongly recommended to only
# route correct hosts to the application.
TRUSTED_HOSTS = ['0.0.0.0', 'localhost', '127.0.0.1']

# Flask-SQLAlchemy
# ================
# See https://flask-sqlalchemy.palletsprojects.com/en/2.x/config/

# TODO: Set
SQLALCHEMY_DATABASE_URI = "postgresql+psycopg2://datasafe-rdm:datasafe-rdm@localhost/datasafe-rdm"
SQLALCHEMY_ENGINE_OPTIONS = {"pool_pre_ping": True}

# Invenio-Base
# ===========
# See https://invenio-base.readthedocs.io/en/latest/

# https://github.com/inveniosoftware/invenio-base/blob/master/invenio_base/wsgi.py#L50
if environ.get('INVENIO_PROXY_FIX_CONFIG', {}):
    PROXYFIX_CONFIG = literal_eval(environ['INVENIO_PROXY_FIX_CONFIG'])

# Invenio-App
# ===========
# See https://invenio-app.readthedocs.io/en/latest/configuration.html

APP_DEFAULT_SECURE_HEADERS = {
    'content_security_policy': {
        'default-src': [
            "'self'",
            'data:',  # for fonts
            "'unsafe-inline'",  # for inline scripts and styles
            "blob:",  # for pdf preview
            "'wasm-unsafe-eval'",  # for uppy file uploader
        ],
    },
    'content_security_policy_report_only': False,
    'content_security_policy_report_uri': None,
    'force_file_save': False,
    'force_https': True,
    'force_https_permanent': False,
    'frame_options': 'sameorigin',
    'frame_options_allow_from': None,
    'session_cookie_http_only': True,
    'session_cookie_secure': True,
    'strict_transport_security': True,
    'strict_transport_security_include_subdomains': True,
    'strict_transport_security_max_age': 31556926,  # One year in seconds
    'strict_transport_security_preload': False,
}

# Flask-Babel
# ===========
# See https://python-babel.github.io/flask-babel/#configuration

# Default locale (language)
BABEL_DEFAULT_LOCALE = 'en'
# Default time zone
BABEL_DEFAULT_TIMEZONE = 'Europe/Berlin'

# Invenio-I18N
# ============
# See https://invenio-i18n.readthedocs.io/en/latest/configuration.html

# Other supported languages (do not include BABEL_DEFAULT_LOCALE in list).
I18N_LANGUAGES = [
    ('de', _('German')),
]

# Invenio-Theme
# =============
# See https://invenio-theme.readthedocs.io/en/latest/configuration.html

# Frontpage title
THEME_FRONTPAGE_TITLE = "datasafe"
# Frontpage Info Banner
INFO_BANNER = ""
# Theme site name
THEME_SITENAME = "datasafe"
# Header logo
THEME_LOGO = 'images/datasafe_logo.svg'
THEME_SHOW_FRONTPAGE_INTRO_SECTION = False
# Header template
THEME_HEADER_TEMPLATE = "invenio_app_rdm/header.html"
# Footer template
THEME_FOOTER_TEMPLATE = "invenio_app_rdm/footer.html"

# Invenio-App-RDM
# ===============
# See https://invenio-app-rdm.readthedocs.io/en/latest/configuration.html

# Instance's theme entrypoint file. Path relative to the ``assets/`` folder.
INSTANCE_THEME_FILE = './less/theme.less'

# Invenio-Files-Rest
# ==================
FILES_REST_STORAGE_FACTORY = 'invenio_s3.s3fs_storage_factory'
FILES_REST_DEFAULT_QUOTA_SIZE = 5 * 10 ** 12
FILES_REST_DEFAULT_MAX_FILE_SIZE = FILES_REST_DEFAULT_QUOTA_SIZE

# Invenio-S3
# ==========
S3_ENDPOINT_URL = environ.get('INVENIO_S3_ENDPOINT_URL', 'http://localhost:9000/')
S3_ACCESS_KEY_ID = 'admin'
S3_SECRET_ACCESS_KEY = '12345678'

# Allow S3 endpoint in the CSP rules
APP_DEFAULT_SECURE_HEADERS['content_security_policy']['default-src'].append(
    S3_ENDPOINT_URL
)

# Invenio File uploader
# =====================
RDM_FILES_DEFAULT_QUOTA_SIZE = 5 * 10 ** 12
RDM_FILES_DEFAULT_MAX_FILE_SIZE = 5 * 10 ** 12
APP_RDM_DEPOSIT_NG_FILES_UI_ENABLED = True

APP_RDM_DEPOSIT_FORM_QUOTA = {
    "maxFiles": 100,
    "maxStorage": 5 * 10 ** 12,
}

# Invenio-Records-Resources
# =========================
# See https://github.com/inveniosoftware/invenio-records-resources/blob/master/invenio_records_resources/config.py

# TODO: Set with your own hostname when deploying to production
SITE_UI_URL = "https://127.0.0.1"

SITE_API_URL = "https://127.0.0.1/api"

RDM_ALLOW_METADATA_ONLY_RECORDS = False;
APP_RDM_DEPOSIT_FORM_DEFAULTS = {
    "publication_date": lambda: datetime.now().strftime("%Y-%m-%d"),
    "resource_type": "dataset",
    "publisher": "datasafe-rdm",
}

# See https://github.com/inveniosoftware/invenio-app-rdm/blob/master/invenio_app_rdm/config.py
APP_RDM_DEPOSIT_FORM_AUTOCOMPLETE_NAMES = 'search'  # "search_only" or "off"

# Invenio-RDM-Records
# ===================
# See https://inveniordm.docs.cern.ch/customize/dois/
DATACITE_ENABLED = False
DATACITE_USERNAME = ""
DATACITE_PASSWORD = ""
DATACITE_PREFIX = ""
DATACITE_TEST_MODE = True
DATACITE_DATACENTER_SYMBOL = ""

# Authentication - Invenio-Accounts and Invenio-OAuthclient
# =========================================================
# See: https://inveniordm.docs.cern.ch/customize/authentication/

# Invenio-Accounts
# ----------------
# See https://github.com/inveniosoftware/invenio-accounts/blob/master/invenio_accounts/config.py
ACCOUNTS_LOCAL_LOGIN_ENABLED = True  # enable local login
SECURITY_REGISTERABLE = False  # local login: allow users to register
SECURITY_RECOVERABLE = False  # local login: allow users to reset the password
SECURITY_CHANGEABLE = False  # local login: allow users to change psw
SECURITY_CONFIRMABLE = False  # local login: users can confirm e-mail address
SECURITY_LOGIN_WITHOUT_CONFIRMATION = True  # Setting this to false will require users to confirm email before being able to login

# Invenio-OAuthclient
# -------------------
# See https://github.com/inveniosoftware/invenio-oauthclient/blob/master/invenio_oauthclient/config.py

# TODO: Configure OAuthclient remote apps here if you want SSO/OIDC login.
# See https://inveniordm.docs.cern.ch/customize/authentication/
OAUTHCLIENT_REMOTE_APPS = dict()

# Invenio-UserProfiles
# --------------------
USERPROFILES_READ_ONLY = True  # allow users to change profile info (name, email, etc...)

# OAI-PMH
# =======
# See https://github.com/inveniosoftware/invenio-oaiserver/blob/master/invenio_oaiserver/config.py

OAISERVER_ID_PREFIX = "datasafe-rdm.com"
"""The prefix that will be applied to the generated OAI-PMH ids."""

# Invenio-Search
# --------------

SEARCH_INDEX_PREFIX = "datasafe-rdm-"

# Allow e-mails to be sent. Defaults to True (suppressed) for this public template; set to False
# once MAIL_SERVER/MAIL_DEFAULT_SENDER below are configured for your own instance.
MAIL_SUPPRESS_SEND = True
# Configured SMTP server's host
MAIL_SERVER = '127.0.0.1'
# Configured SMTP server's port
MAIL_PORT = 1025
# TODO: Set to your own contact address
MAIL_DEFAULT_SENDER = "support@example.org"
MAIL_DEFAULT_REPLY_TO = "support@example.org"
SECURITY_EMAIL_SENDER = "support@example.org"

ACCOUNTS_DEFAULT_USER_VISIBILITY = "public"
ACCOUNTS_DEFAULT_EMAIL_VISIBILITY = "public"

# Create Admin User
RDM_RECORDS_USER_FIXTURE_PASSWORDS = {'admin@inveniosoftware.org': '123456'}

# Setttings for invenio-pdf-geerator
from invenio_app_rdm.config import (
    NOTIFICATIONS_BACKENDS,
    NOTIFICATIONS_BUILDERS,
)
from invenio_pdf_generator.backends import EmailWithPdfAttachmentNotificationBackend
from invenio_pdf_generator.notifications.builders import (
    CommunityInclusionAcceptNGNotificationBuilder,
    CommunityInclusionSubmittedNGNotificationBuilder,
)
from invenio_rdm_records.notifications.builders import (
    CommunityInclusionAcceptNotificationBuilder,
    CommunityInclusionSubmittedNotificationBuilder,
)

NOTIFICATIONS_BACKENDS = {
    **NOTIFICATIONS_BACKENDS,
    EmailWithPdfAttachmentNotificationBackend.id: EmailWithPdfAttachmentNotificationBackend(),
}

NOTIFICATIONS_BUILDERS = {
    **NOTIFICATIONS_BUILDERS,
    CommunityInclusionAcceptNotificationBuilder.type: CommunityInclusionAcceptNGNotificationBuilder,
    CommunityInclusionSubmittedNotificationBuilder.type: CommunityInclusionSubmittedNGNotificationBuilder,
}

# Celery settings
CELERY_ACCEPT_CONTENT = ["json", "msgpack", "yaml"]
CELERY_TASK_SERIALIZER = "msgpack"
CELERY_RESULT_SERIALIZER = "msgpack"
BROKER_URL = "redis://localhost:6379/5"
CELERY_BROKER_URL = "redis://localhost:6379/5"

# --------------------------------------------------------
# Planned purge date custom field
# --------------------------------------------------------

RDM_NAMESPACES = {
    # TODO: Set to a namespace URI you control
    "datasafe": "https://example.org/datasafe",
}

from invenio_records_resources.services.custom_fields import ISODateStringCF

RDM_CUSTOM_FIELDS = [
    ISODateStringCF(
        name="datasafe:planned_purge_date",
    )
]

# --------------------------------------------------------
# PDF Receipt
# --------------------------------------------------------

from invenio_pdf_generator.components import ReceiptComponent
from invenio_purge_expired.components import PlannedPurgeComponent
from invenio_rdm_records.services.components import DefaultRecordsComponents

RDM_RECORDS_SERVICE_COMPONENTS = DefaultRecordsComponents + [PlannedPurgeComponent, ReceiptComponent]

# --------------------------------------------------------
# Custom drafts search options
# --------------------------------------------------------

from invenio_rdm_records.services.config import RDMRecordServiceConfig
from invenio_records_resources.services.base.config import FromConfigSearchOptions
from datasafe_rdm.custom_drafts_search import CustomRDMSearchDraftsOptions

RDMRecordServiceConfig.search_drafts = FromConfigSearchOptions(
    "RDM_SEARCH_DRAFTS",
    "RDM_SORT_OPTIONS",
    "RDM_FACETS",
    search_option_cls=CustomRDMSearchDraftsOptions,
)

# --------------------------------------------------------
# Route modifications
# --------------------------------------------------------

from invenio_app_rdm.config import APP_RDM_ROUTES, APP_RDM_USER_DASHBOARD_ROUTES
from datasafe_rdm.views import redirect_to_dashboard

APP_RDM_ROUTES["index"] = ("/", redirect_to_dashboard)

APP_RDM_USER_DASHBOARD_ROUTES["uploads"] = "/me/home"

# --------------------------------------------------------
# Record Landing Page
# --------------------------------------------------------


APP_RDM_DETAIL_SIDE_BAR_TEMPLATES = [
    "invenio_app_rdm/records/details/side_bar/extend_storage_period.html",
    "invenio_app_rdm/records/details/side_bar/versions.html",
    "datasafe_rdm/access_rights.html",
    "invenio_app_rdm/records/details/side_bar/keywords_subjects.html",
]

# --------------------------------------------------------
# Static pages
# --------------------------------------------------------

APP_RDM_PAGES = {
    "frequently-asked-questions": "/faq",
    "terms-and-conditions": "/terms",
    "credits": "/credits",
}

PAGES_DEFAULT_TEMPLATE = "datasafe_rdm/datasafe_rdm_static_page_template.html"

PAGES_TEMPLATES = [
    ("invenio_pages/dynamic.html", "Default dynamic"),
    ("datasafe_rdm/datasafe_rdm_static_page_template.html", "Default")
]
PDF_GENERATOR_NAME_PREFIX = "Zertifikat_datasafe_"

WEBPACKEXT_PROJECT = "invenio_assets.webpack:rspack_project"

I18N_TRANSIFEX_JS_RESOURCES_MAP = {
    "invenio-administration-messages-ui": "invenio_administration",
    "invenio-app-rdm-messages-ui": "invenio_app_rdm",
    "invenio-communities-messages-ui": "invenio_communities",
    "invenio-jobs-messages-ui": "invenio_jobs",
    "invenio-rdm-records-messages-ui": "invenio_rdm_records",
    "invenio-requests-messages-ui": "invenio_requests",
    "invenio-search-ui-messages-js": "invenio_search_ui",
    "invenio-vocabularies-messages-ui": "invenio_vocabularies"
}

I18N_JS_DISTR_EXCEPTIONAL_PACKAGE_MAP = {
    "jobs": "invenio_jobs",
    "invenio_previewer_theme": "invenio_previewer",
    "invenio_app_rdm_theme": "invenio_app_rdm",
}
