# README

We provide empty funders, names and subjects vocabularies.
We also provide a `languages.yaml` file with a small selection of languages.
Use `invenio vocabularies import --vocabulary funders -o $DEVBOX_PROJECT_ROOT/$DEVBOX_INSTANCE_PATH/app_data/vocabularies/funders.zip` to import our subset of funders.